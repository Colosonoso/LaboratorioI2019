#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* bit que es el minimo valor que se puede usar.
4bytes se necesita en espacio de memoria
char ocupa 1 byte
"" es una mascara de entrada,un tipo de dato
& es la direccion de memoria de la variable
stdin representa buffer de entrada del teclado.standard input
fflush limpia el buffer


Git Gui es una interfaz grafica y bash es la consola
Push sube los repositorios a la web todos los cambios y commit que haya hecho de manera local
en el area roja aparece el que agregue el archivo

rescan permite ver las modificaciones igual que git status y permite ver lo que paso

git add agrega todos los archivos igual que git stage changed

*/
int main()
{
    /*int numero;
    char letra;
    char palabra[10]; /*reserva 10byte consecutivos para guardar letra*/



    /*printf("ingrese una palabra :");
    setbuf(stdin,NULL);
    scanf("%s", palabra);

    printf("La palabra es: %s \n",palabra); cuando uso el espacio no me hace seguir la palabra, es
    como si pusiera un */

   /*

    printf("Ingrese una letra: ");
    //fflush(stdin); se puede usar fflush o setbuf para windows, para Linux SÓLO setbuf
    setbuf(stdin,NULL);
    scanf("%c",&letra);



    printf("El numero es : %d y la letra es: %c \n",numero,letra);
*/
    system("pause");

    return 0;
}
