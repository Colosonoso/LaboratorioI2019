#include <stdio.h>
#include <stdlib.h>

/* C es un lenguaje compilado mientras que JavaScript es un lenguaje interpretado

main.o es un objeto

bin=binario(un archivo ejecutable que tiene el proyecto, es el programa

Para que pueda funcionar bien(que no entre y luego cierre, debemos agregar en el codigo el system("pause")
con eso congelara la consola


*/

int main()
{
    printf("Hello world!\n");

    system("pause");
    return 0;
}
